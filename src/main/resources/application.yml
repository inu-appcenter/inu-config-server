server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/juhwan0815/spring-cloud-config
          default-label: master

management:
  endpoints:
    web:
      exposure:
        include: health
