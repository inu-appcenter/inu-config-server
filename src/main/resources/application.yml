server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/inu-appcenter/inu-config
          default-label: master

management:
  endpoints:
    web:
      exposure:
        include: health
